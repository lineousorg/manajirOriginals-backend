services:
  backend-prod:
    image: lineousreza/lineousrepo:backend-manajir-prod
    container_name: backend-manajir-prod
    restart: always
    ports:
      - "5000:3000"
    environment:
      - NODE_ENV=production
